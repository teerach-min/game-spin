# Nixpacks configuration for Backend Service
[phases.setup]
nixPkgs = ["nodejs-22_12_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npm run prisma:generate",
  "npm run build"
]

[start]
cmd = "npm run start:prod"
